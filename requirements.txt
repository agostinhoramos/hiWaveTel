# Core Django and REST framework dependencies
Django==5.2.1
djangorestframework==3.15.2
djangorestframework-simplejwt==5.5.1

# Built-in setuptools used by tooling and some wheels during install
setuptools>=69.0.0

# IANA time zones for zoneinfo (Django USE_TZ); Docker image also installs OS tzdata
tzdata>=2024.1

# OpenAPI documentation
drf-spectacular==0.29.0

# WSGI server
gunicorn==23.0.0

# ModemManager inbound SMS (system D-Bus)
dbus-next>=0.2.3

# Dev / test tooling
pytest==8.3.5
pytest-django==4.10.0
